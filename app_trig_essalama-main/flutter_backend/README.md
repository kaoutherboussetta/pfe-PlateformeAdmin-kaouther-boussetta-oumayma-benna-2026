# Backend API

Le serveur API (Express + MongoDB) se trouve à la **racine du projet** : `../server.js`.

Pour lancer le backend :
```bash
cd ..
node server.js
```

MongoDB : collection `user_citoyen`, base `trig_essalama`.
